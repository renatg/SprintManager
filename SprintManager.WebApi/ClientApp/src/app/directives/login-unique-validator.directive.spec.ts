import { LoginUniqueValidatorDirective } from './login-unique-validator.directive';

describe('LoginUniqueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginUniqueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
